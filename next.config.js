/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['senaigram.herokuapp.com',
              'senai-instagram.mayckxavier.com'],
  },
}

module.exports = nextConfig
